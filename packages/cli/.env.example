# Use the locally running official site for account, authorization, and cloud-sync development.
# Start it separately with `pnpm dev:site`. Remove this override to use the production site.
SITE_URL=http://localhost:4000
